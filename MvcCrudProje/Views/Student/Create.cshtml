@model MvcCrudProje.Models.Entities.Student
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-12">
        <!-- Horizontal Form -->
        <div class="box box-primary">
            <div class="container">
                <div class="row">
                    <div class="box-header with-border">
                        <h3 class="box-title">Öğrenci Ekle</h3>
                    </div>
                    <!-- /.box-header -->
                    <!-- form start -->
                    <div class="box-body">
                        @using (Html.BeginForm("Insert", "Student", FormMethod.Post, new { @class = "form-horizontal" }))
                        {
                            <div class="form-group">

                                @Html.LabelFor(m => m.Name, new { @class = "col-sm-2 control-label" })
                                <div class="col-sm-10">
                                    @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                                </div>
                            </div>

                            <div class="form-group">

                                @Html.LabelFor(m => m.Surname, new { @class = "col-sm-2 control-label" })
                                <div class="col-sm-10">
                                    @Html.TextBoxFor(m => m.Surname, new { @class = "form-control" })
                                </div>
                            </div>

                            <div class="form-group">

                                @Html.LabelFor(m => m.Age, new { @class = "col-sm-2 control-label" })
                                <div class="col-sm-10">
                                    @Html.TextBoxFor(m => m.Age, new { @class = "form-control" })
                                </div>
                            </div>

                            <div class="form-group">

                                @Html.LabelFor(m => m.Gender, new { @class = "col-sm-2 control-label" })
                                <div class="col-sm-10">
                                    @Html.DropDownListFor(m => m.Gender, new List<SelectListItem>
                                        { new SelectListItem{Text="erkek", Value="erkek"},
                                          new SelectListItem{Text="kadın", Value="kadın"}}, "Bir cinsiyet seçin", new { @class = "form-control" })
                                </div>
                            </div>

                            <div class="form-group">

                                @Html.LabelFor(m => m.teacherID, new { @class = "col-sm-2 control-label" })
                                <div class="col-sm-10">
                                    @Html.TextBoxFor(m => m.teacherID, new { @class = "form-control" })
                                </div>
                            </div>


                            <div class="box-footer">
                                <button type="submit" class="btn btn-info pull-right">Kaydet</button>
                            </div>
                        }
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>

